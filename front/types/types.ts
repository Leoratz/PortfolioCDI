

export interface Project {
  title: string;
  details: string;
  link: string;
  image?: string;
}
export interface listePrograms {
  title: string;
  description: string;
  icon: React.ReactNode;
}